'use client'

export default function Contact(){
    return(
    <section id="contact" className="h-screen flex justify-center items-center bg-slate-600">
        <h2 className="text-white">this is Contact section</h2>
    </section>
    );
}